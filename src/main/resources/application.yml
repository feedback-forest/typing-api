spring:
  # Basic Config
  application:
    name: typing

  profiles:
    default: local

  # JPA Config
  jpa:
    show-sql: false
    database: mysql
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        format_sql: true

# Logging config
logging:
  level:
    root: INFO
